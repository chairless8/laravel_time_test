services:
  compression-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: compression-service-app
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - .:/var/www/html
    depends_on:
      compression-db:
        condition: service_healthy

  compression-web:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "8000:80"
    volumes:
      - .:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - compression-app

  compression-db:
    image: mysql:8.0
    restart: unless-stopped
    ports:
      - "33066:3306"
    environment:
      MYSQL_DATABASE: compression_service
      MYSQL_USER: laravel_user
      MYSQL_PASSWORD: laravel_password
      MYSQL_ROOT_PASSWORD: root_password
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-uroot", "-proot_password"]
      interval: 5s
      timeout: 5s
      retries: 5

  compression-queue:
    build:
      context: .
      dockerfile: Dockerfile
    image: compression-service-app
    restart: unless-stopped
    command: php artisan queue:work --verbose --tries=3 --timeout=90
    working_dir: /var/www/html
    volumes:
      - .:/var/www/html
    depends_on:
      compression-db:
        condition: service_healthy
      compression-app:
        condition: service_started

volumes:
  db_data:
